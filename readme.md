
## 📌 Project Structure

This repository is organized into multiple directories, each representing a phase 

NOTE THAT
---

## 📁 Important Rule

📄 **Each directory contains its own `README.md` file** that explains:

- The files and roles of each file

➡️ To understand what a file does, always read the **`README.md` inside its directory first**.

---

## 🧭 How to Navigate the Project

1. Start from this root directory  
2. Enter a task folder (e.g. `Task1`, `Task2`, `Task3`, `web_app` )  
3. Open the `README.md` inside that folder  
4. Then explore the scripts and notebooks

This keeps the project easy to understand, even with many experiments and versions.

---

## 🧠 Project Goal

The overall objective of this project is to build a **fire detection system** using:

- Apply some machine learning to try to predict the presence of fire

---

(YOU CAN ignore the files here and enter the directories)
